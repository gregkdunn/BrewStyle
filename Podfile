# Using Cocoapods to add Obj-C frameworks to the project
# Adding swift frameworks directly to the project due to Parse
platform :ios, '9.0'

target 'BrewStyle' do
	pod 'Parse'
	pod 'ParseUI'
	pod 'pop', '~> 1.0'
end
